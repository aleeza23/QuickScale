import React from 'react'

const SectionHeading = ({heading}) => {
    return (
        <div className='font-mattone text-center font-bold text-xl lg:text-3xl'>{heading}</div>
    )
}

export default SectionHeading